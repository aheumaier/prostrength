<%= form_with(model: tempo) do |form| %>
  <% if tempo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tempo.errors.count, "error") %> prohibited this tempo from being saved:</h2>

      <ul>
        <% tempo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :duration, style: "display: block" %>
    <%= form.text_field :duration %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
